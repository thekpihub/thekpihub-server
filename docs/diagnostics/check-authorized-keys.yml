# ─────────────────────────────────────────────────────────────────────────────
# ARCHIVED, INERT — moved out of .github/workflows/ on 2026-09-04 per user
# request (kept for the record, not deleted). No longer dispatchable as a
# GitHub Actions workflow from this location.
#
# Ran once, 2026-09-04 (run 33821623751): checked ~/.ssh/authorized_keys on
# the Hostinger production server, read-only, for any key installed by the
# 2026-08-27 kpihub-migration-bootstrap.yml workflow (from the deleted
# hsharmagxi-debug/thekpihub-platform repo), per the RE-AUDIT FINDINGS
# action item in C:\Projects\CLAUDE.md.
#
# RESULT: confirmed still present. authorized_keys had 5 entries / 4 unique
# keys as of that run, file last-modified 2026-08-27 18:18:17 UTC:
#   - SHA256:/74maGoMj/JXmLKP2YynqTuA27/Cn0zPNx81SqOYiDg  (RSA 3072, no comment)
#   - SHA256:Jhq8DLJy/ECKhr6dCAEBqz3yNcYdTL+RP8CFTUimThs  "Admin@GXIPC-Himanshu" (ED25519, listed twice — duplicate entry)
#   - SHA256:fKFfaRvVh7kcGp0BrL4P/d1WmQ4VgP59x/e1lBszZNk  "GitHub Actions Hostinger Deployment" (ED25519, the legitimate deploy key)
#   - SHA256:+Y+jfdBpzHid4qBt3sasZ/7XQRXhN7I4mzTjlmYL8ls  "kpihub-assembled-hostinger-2026-08-27" (ED25519, THE MIGRATION KEY — timestamp matches its install exactly)
#
# Not removed as of this archiving — that decision was left to the user.
# See servermemory.md for the session log this belongs to.
# ─────────────────────────────────────────────────────────────────────────────
name: TEMP - check authorized_keys

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    name: Read-only authorized_keys check
    runs-on: ubuntu-latest
    environment: hostinger-production
    env:
      HOSTINGER_HOST: ${{ secrets.HOSTINGER_HOST }}
      HOSTINGER_PORT: ${{ secrets.HOSTINGER_PORT }}
      HOSTINGER_USERNAME: ${{ secrets.HOSTINGER_USERNAME }}
      HOSTINGER_SSH_KEY: ${{ secrets.HOSTINGER_SSH_KEY }}
      HOSTINGER_SSH_KNOWN_HOSTS: ${{ secrets.HOSTINGER_SSH_KNOWN_HOSTS }}
    steps:
      - name: Configure verified SSH
        run: |
          test -n "$HOSTINGER_HOST"
          test -n "$HOSTINGER_PORT"
          test -n "$HOSTINGER_USERNAME"
          test -n "$HOSTINGER_SSH_KEY"
          test -n "$HOSTINGER_SSH_KNOWN_HOSTS"
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$HOSTINGER_SSH_KEY" > ~/.ssh/hostinger_deploy_key
          printf '%s\n' "$HOSTINGER_SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/hostinger_deploy_key
          chmod 644 ~/.ssh/known_hosts

      - name: List authorized_keys (fingerprints + comments only, no key material)
        run: |
          ssh -i ~/.ssh/hostinger_deploy_key -p "$HOSTINGER_PORT" \
            -o StrictHostKeyChecking=yes \
            "$HOSTINGER_USERNAME@$HOSTINGER_HOST" \
            'echo "--- authorized_keys entry count ---"; \
             wc -l < ~/.ssh/authorized_keys 2>/dev/null || echo "no authorized_keys file"; \
             echo "--- fingerprints + comments (no raw key material) ---"; \
             ssh-keygen -lf ~/.ssh/authorized_keys 2>/dev/null || echo "could not fingerprint"; \
             echo "--- last-modified time ---"; \
             stat -c "%y %n" ~/.ssh/authorized_keys 2>/dev/null || stat -f "%Sm %N" ~/.ssh/authorized_keys 2>/dev/null'
