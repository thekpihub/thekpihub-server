# Assembly Report

## Discovery Summary

The authenticated GitHub account is `hsharmagxi-debug`. The organization
`thekpihub` is accessible. Relevant repositories were inspected across the
organization and personal account.

## Keep / Merge / Reference / Ignore

- KEEP: `thekpihub-website`, `thekpihub-platform`, `thekpihub-pipeline`,
  `automated-website-builder`.
- MERGE: `thekpihub-app` for legacy app, backend, Prisma, and SQL migrations.
- ARCHIVE-REFERENCE: archived `thekpihub`, `kpihub-vault`.
- REFERENCE: `thekpihub-wingcommander-design-sync`, `thekpihub-wing-commander`,
  `ditto-wingman`, personal mirrors.
- IGNORE: empty `hsharmagxi-debug/ditto-wingman`.


## Duplicate Copy Policy

The assembled repository keeps one physical copy of each selected component. Backup payloads, duplicate reference folders, central database mirror copies, and packaged ZIP artifacts were pruned. Their source locations remain documented in provenance instead of being copied twice.

## Validation Status

Validation results are recorded in `docs/VALIDATION.md`.

## Security Status

Real `.env`, `config.js`, and `credentials.md` files were excluded from copying.
Security scan results are recorded in `docs/VALIDATION.md`.

